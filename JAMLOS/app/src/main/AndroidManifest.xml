<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jamlos">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Activity.User.AccountActivity"></activity>
        <activity android:name=".Activity.User.CartActivity" />
        <activity android:name=".Activity.User.ProductActivity" />
        <activity android:name=".Activity.User.CategoryActivity" />
        <activity android:name=".Activity.User.SaleActivity" />
        <activity android:name=".Activity.User.SignUpActivity" />
        <activity android:name=".Activity.User.HomeActivity" />
        <activity android:name=".Activity.User.DetailProductActivity" />
        <activity
            android:name=".Activity.User.LogInActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>